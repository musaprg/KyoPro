#include <bits/stdc++.h>

#define ll long long int
#define INFTY (1<<21)

using namespace std;

int main(void){
    cin.sync_with_stdio(false);
    string S;
    cin>>S;
    if(S[S.size()-1] == 'T') cout<<"YES"<<endl;
    else cout<<"NO"<<endl;
    return 0;
}

#include <bits/stdc++.h>

#define ll long long int
#define INFTY (1<<21)

using namespace std;

int main(void){
    cin.sync_with_stdio(false);
    vector<int> A;
    int N,S,T,W; cin>>N>>S>>T>>W;
    for(int i=0;i<N-1;i++){
        int a; cin>>a;
        A.push_back(a);
    }
    

    return 0;
}
